class Solution {
public:
    bool isPalindrome(int x) {
        int b=x;
        long long int sum=0;
        while(x!=0){
          int j=x%10;
          sum=(sum*10)+j;
          x=x/10;
        }
        if(sum==abs(b))return true;
        else return false;
    }
};
