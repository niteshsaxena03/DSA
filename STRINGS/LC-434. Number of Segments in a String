class Solution {
public:
    int countSegments(string s) {
        //breaking string into differnt words
        stringstream ss(s);
        //empty string for comparision
        string str;
        //storing count
        int count=0;
        while(ss>>str) count++;
        return count;
    }
};
